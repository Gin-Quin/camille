{
	"name": "@camille/workspace",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"dev:client": "cd client && bunx --bun vite dev",
		"dev:server": "cd server && bun run --hot src/index.ts",
		"build:client": "cd client && bunx --bun vite build"
	},
	"devDependencies": {
		"@prisma/client": "^6.5.0",
		"prisma": "^6.5.0",
		"prettier": "^3.5.3"
	},
	"dependencies": {
		"gsap": "^3.12.7"
	}
}
